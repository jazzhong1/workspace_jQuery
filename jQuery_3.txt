================================클래스/속성==========================================

-------------------------------------------------------------addClass()----------------------------------------------------
addClass()
선택자에 의해 선택된 요소에 클래스를 추가하는 메소드

1. $(‘s’).addClass(“클래스이름”);
//클래스를 여러 개 추가는 띄어쓰기로 구분

2. $(‘s’).addClass(function(n){
인자는 선택된 요소들의 번호;
return 값; // 리턴된 값을 클래스로 추가
});
-------------------------------------------------------------removeClass()----------------------------------------------------

